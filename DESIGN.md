# 声页 UI / UX 设计规范

## 设计场景

用户在夜间的书桌前戴耳机，连续校听多章小说。默认使用墨黑工作面降低周围亮度变化，以淡黄绿主控制键和克制的青色音轨标出可操作内容；大段正文保持舒适字号和行距。提供亮色主题适应白天，由侧边栏底部按钮切换并记住选择；两套主题共享同一套语义令牌（oklch），正文对比度均满足 4.5:1。

## 主题与动效

- 颜色全部收敛为语义令牌：结构色（背景/面板/线条/文本/主色/语义色）随主题切换；封面插画、登录页品牌绿、声纹图与阴影保持固定，两种主题下都成立（登录页品牌面板有专门的亮色覆盖）。
- 动效只用于状态与反馈：页面进入淡入 260ms、列表逐项错峰淡入、对话框与 Toast 出现（毛玻璃背板）、按钮按压、播放键在播放中缓慢呼吸、当前段落高亮、书卡悬停抬升与封面高光掠过、运行中任务进度条轻微呼吸。
- 背景有两片极低饱和的光晕缓慢漂移（70s 循环、仅 transform），为长时间工作提供最低限度的生命感；`prefers-reduced-motion` 下全部动画停用（含 JS 侧的平滑滚动）。除此之外不增加与内容无关的循环光晕。

## 视觉识别

标志：三条高低不同的声音竖线与一本书的合页关系，搭配“声页”。暂定英文内部代号 Soundleaf，不用于替代中文导航。

特色来自章节编号、封面竖向比例、音轨刻度、真实波形、清晰的播放头。书架采用大封面与紧凑制作清单；导演页采用目录/脚本/参数三栏；试听页充分留白。不同页面承担不同任务，避免全部铺成同样的卡片网格。

## 色彩令牌

```css
:root {
  --bg: oklch(0.145 0.006 110);
  --surface: oklch(0.19 0.007 110);
  --raised: oklch(0.235 0.008 110);
  --line: oklch(0.32 0.008 110);
  --text: oklch(0.95 0.008 110);
  --muted: oklch(0.73 0.012 110);
  --primary: oklch(0.9 0.135 110);
  --on-primary: oklch(0.19 0.01 110);
  --accent: oklch(0.76 0.1 180);
  --warning: oklch(0.82 0.12 75);
  --danger: oklch(0.76 0.14 25);
}
```

主按钮使用淡黄绿配深色字；状态用文字与图标辅助，不单靠颜色。强调色只用于动作、选中和音频轨道。AI 概念图以近似颜色呈现，不是精确色值验收样本。

## 字体与布局

- UI：系统中文无衬线，14–16px；标题 28–32px；时间码使用等宽数字。
- 小说段落：18px、1.9 行高、每行约 28–38 个汉字；书封可用衬线字体现文学质感。
- 桌面基准 1440×960：左导航 216px；顶部 72px；主区内边距 32px；播放器 80px。
- 间距以 4px 为基准取值（常用 8/12/16/24/32，正文区允许内容微调）；控件约 42px 高（40–44 区间）；圆角 4–13px，随控件层级递增；封面采用 2:3。
- 导演页：224px 章节栏 + 弹性正文 + 288px 参数栏。1024px 时参数改抽屉；小于 768px 采用章节、正文、参数分步标签。
- 手机书架双列封面，表格变纵向条目，固定播放器只保留核心控制；复杂编辑可完成但优先桌面。

## 关键页面构图

1. 书架：左导航，顶部标题与“导入小说”，上方继续制作横区，下方 4 本封面与制作清单。
2. 导入：顶部步骤条，左侧文件/编码信息，下方可修正目录，右侧长正文预览；底部固定确认动作。
3. 工作台：书封与书名横区、完成进度、章节表、批量动作；每章有试听、导演和更多菜单。
4. AI 导演：三栏，角色颜色细边与情绪标签；底部片段音轨；选中片段才显示右侧参数。
5. 音色库：筛选行、紧凑音色列表与声纹小图；右侧选中音色详情及统一试听文本。不要用人像暗示真实配音演员。
6. 队列：生成中项目置顶，分阶段进度与计数，失败任务有就地原因及恢复入口。
7. 试听：左侧书封和章节，中央大波形与播放按钮，下方当前段落和问题标记；播放倍速明确命名。
8. 导出：左侧章节勾选，右侧格式与文件名预览，下方导出历史；显示版本与遗漏章。
9. 设置：服务/AI/存储/账户标签，正文表单与连接结果；首次设置以相同视觉提供三步向导。

## 交互与状态

- 生成前显示“选中 3 章 · 预计处理 8,420 字”，费用无价格资料时显示“价格未配置”，禁止伪造精确金额。
- 保存显示“已保存”或“保存失败，重试”，不能只闪一下 Toast。
- AI 建议以“建议”标签、原因与接受/撤回呈现。只读原文与朗读替换分开。
- 更新情绪/音色后标记音频待更新，并保留旧版播放入口。
- “暂停队列”只暂停领取新任务；“取消本章”在安全边界结束当前任务，已提交请求可能仍完成。
- 试听按钮加载时可取消，失败显示原因；多个试听共享同一播放器。
- 内容加载用骨架；音频生成用已完成片段数，不用虚假匀速进度。
- 主体页面不用弹窗承载；确认删除、费用范围与附属首次设置允许原生 dialog。

## 动效和无障碍

hover/选中切换 160–200ms，只改变透明度或颜色；播放时真实音量反馈可动，暂停即静止。不增加与内容无关的循环光晕。尊重 prefers-reduced-motion。

所有图标按钮有可访问名称；表单有显式 label；焦点对比清楚；对话框可 Escape 关闭并返回触发按钮（首次登录的默认密码修改弹窗例外：必须完成后才能进入工作室）；状态通知使用 aria-live。空格仅在非输入控件区域控制播放。播放器有文字时间、可键盘拖动进度和音量。

## 原型边界

原型可以切换九页、选择章节、筛选声音、调整导演参数、模拟任务与导出清单，并允许上传本地音频验证播放器。未连接 TTS，语音试听按钮提供明确演示说明，不能用蜂鸣声冒充小说配音。导出演示仅输出选择清单，不伪装为真实 MP3。

## 图稿交付

九张 AI 生成图对应九个主页面，统一风格；首次设置、登录、失败/空状态由原型与规范补齐。图片中的音色昵称为演示，不等同于供应商的正式音色 ID。
